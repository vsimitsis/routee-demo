{
    "version": 2020070201,
    "cityId": "The city ID from the OpenWeather list",
    "recipient": "The mobile number of the recipient",
    "temp": {
        "threshold": "(Integer)The max temperature for the SMS."
    },
    "api": {
        "routee": {
            "id": "Routee APP ID",
            "key": "Routee Secret Key",
            "sms-endpoint": "Routee SMS endpoint"
        },
        "openweather": {
            "key": "OpenWeather API Key",
            "endpoint": "OpenWeather Endpoint"
        }
    }
}
